import type { Messages } from "./messages";

const ja: Messages = {
  untitled: "チャート",
  chartCount: (count) => `${count} チャート`,
  chartTitle: (num) => `チャート ${num}`,
  png: "PNG",
};

export default ja;
