import type { Messages } from "./messages";

const ko: Messages = {
  untitled: "차트",
  chartCount: (count) => `${count}개 차트`,
  chartTitle: (num) => `차트 ${num}`,
  png: "PNG",
};

export default ko;
