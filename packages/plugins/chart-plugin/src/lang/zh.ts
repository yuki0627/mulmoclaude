import type { Messages } from "./messages";

const zh: Messages = {
  untitled: "图表",
  chartCount: (count) => `${count} 个图表`,
  chartTitle: (num) => `图表 ${num}`,
  png: "PNG",
};

export default zh;
